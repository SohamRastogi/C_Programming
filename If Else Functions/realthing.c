#include <stdio.h>
int main()
{
    int x=3;
    if(x=2) printf("hello world");        /*reassignment in if. Print hoga and value of 
    // else printf("goodbye world");        x would be 2. So if we now print the value
    return 0;                               of x then x => 2. */
    
}