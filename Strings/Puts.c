#include <stdio.h>
#include <string.h>
int main()
{
    char str[] = "College Wallah is best";
    puts(str);                              //automatic '\n' !!
    puts("hello everyone");

    return 0;
}
